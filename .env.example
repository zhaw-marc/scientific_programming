# Copy this file to .env (same folder as requirements.txt) and set your key.
OPENAI_API_KEY=

# PostgreSQL (Docker Compose — hostname 'db' resolves inside Codespaces)
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=scidb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=geheim